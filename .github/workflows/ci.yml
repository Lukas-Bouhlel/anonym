name: CI Workflow

on: 
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  # security:
  #   runs-on: ubuntu-latest

  #   steps:
  #     # Checkout the repository
  #     - name: Checkout repository
  #       uses: actions/checkout@v3

  #     # ---- Frontend Snyk Security Check ----
  #     - name: Run Snyk on Frontend for vulnerabilities
  #       working-directory: ./anonym-front-end
  #       run: npx snyk test --severity-threshold=high
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  #     # ---- Backend Snyk Security Check ----
  #     - name: Run Snyk on Backend for vulnerabilities
  #       working-directory: ./anonym-back-end
  #       run: npx snyk test --severity-threshold=high
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: test_anonym
        ports:
          - '3306:3306'
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    # Checkout the repository
    - name: Checkout repository
      uses: actions/checkout@v3

    # Set up Node.js
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20.16.0'

    # # ---- Frontend: Lint, Test, and Build ----
    # - name: Install dependencies for Frontend
    #   working-directory: ./anonym-front-end   # Path to your front-end folder
    #   run: npm install

    # - name: Run Frontend Lint
    #   working-directory: ./anonym-front-end   # Lint JavaScript files
    #   run: npm run lint

    # - name: Run Frontend SCSS Lint
    #   working-directory: ./anonym-front-end   # Lint SCSS files
    #   run: npm run lint:scss

    # - name: Run Frontend Tests
    #   working-directory: ./anonym-front-end
    #   run: npm run test -- --ci

    # - name: Build Frontend
    #   working-directory: ./anonym-front-end
    #   run: npm run build

    # ---- Backend: Lint, Test, Database Setup, and Test Execution ----
    - name: Install dependencies for Backend
      working-directory: ./anonym-back-end
      run: npm install

    # Start MySQL container
    - name: Set up MySQL for testing
      uses: docker/setup-buildx-action@v2

    # Wait for MySQL to be ready
    - name: Wait for MySQL to be ready
      run: |
        while ! mysqladmin ping -h 127.0.0.1 --port 3306 -u root -ppassword --silent; do
          sleep 1
        done

    # Create the test database
    - name: Create Test Database
      run: |
        mysql -h 127.0.0.1 --port 3306 -u root -ppassword -e 'CREATE DATABASE IF NOT EXISTS test_anonym;'

      # ---- Créer la base de données de test ----
    - name: Create Test Database
      working-directory: ./anonym-back-end
      run: npx sequelize-cli db:create --env test --config ./app/config/config.js
      env:
        DB_TEST_USERNAME: root
        DB_TEST_PASSWORD: ""
        DB_TEST_DATABASE: test_anonym
        DB_TEST_HOST: 127.0.0.1
        DB_TEST_DIALECT: mysql
        DB_PORT: 3306
  
    # ---- Appliquer les migrations sur la base de données de test ----
    - name: Run Migrations on Test Database
      working-directory: ./anonym-back-end
      run: npx sequelize-cli db:migrate --env test --config ./app/config/config.js
      env:
        DB_TEST_USERNAME: root
        DB_TEST_PASSWORD: ""
        DB_TEST_DATABASE: test_anonym
        DB_TEST_HOST: 127.0.0.1
        DB_TEST_DIALECT: mysql

    # ---- Lancer les tests backend ----
    - name: Run Backend Tests
      working-directory: ./anonym-back-end
      run: npm run test
      env:
        NODE_ENV: test
        DB_TEST_USERNAME: root
        DB_TEST_PASSWORD: ""
        DB_TEST_DATABASE: test_anonym
        DB_TEST_HOST: 127.0.0.1
        DB_TEST_DIALECT: mysql